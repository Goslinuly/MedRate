# Copy to .env and fill in. Loaded by app.py at startup.
# Get a free key at https://aistudio.google.com -> "Get API key".
GEMINI_API_KEY=

# Optional — defaults to gemini-2.5-flash (free tier, supports vision + JSON).
MODEL=gemini-2.5-flash
